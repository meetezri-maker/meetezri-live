{
  "version": 2,
  "installCommand": "corepack enable && corepack prepare pnpm@10.33.0 --activate && cd \"$VERCEL_GIT_REPO_ROOT\" && pnpm install --no-frozen-lockfile",
  "buildCommand": "pnpm run build",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index" }
  ]
}
