{
  "version": 2,
  "installCommand": "cd \"$VERCEL_GIT_REPO_ROOT\" && npm install -g @pnpm/exe@10.33.0 && pnpm install --no-frozen-lockfile",
  "buildCommand": "pnpm run build",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index" }
  ]
}
